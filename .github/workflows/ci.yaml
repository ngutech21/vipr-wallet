name: Build Frontend

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions: read-all

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Dependency review
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions/dependency-review-action@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.28.0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        # Enforce deterministic installs from pnpm-lock.yaml:
        # fail CI if package.json and lockfile are out of sync.
        # also fail on peer dependency incompatibilities.
        run: pnpm install --frozen-lockfile --strict-peer-dependencies

      - name: Check code formatting
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm typecheck

      - name: Type check PWA
        run: pnpm typecheck:pwa

      - name: Audit production dependencies
        run: pnpm audit --prod

      - name: Check circular dependencies
        run: pnpm dlx madge src --circular

      - name: Build frontend
        run: pnpm build
      
      - name: Run unit tests
        run: pnpm test:unit:ci

      - name: Generate coverage report
        run: pnpm coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          verbose: true

  docker-build-pr:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Build Docker image
        run: docker build --file Dockerfile --tag vipr-wallet:pr-check .
