# rename to .env.local and fill in the values
# used for local testing with self signed certificates
# (do not track this file in git)
HTTPS_KEY=certs/localhost+2-key.pem
HTTPS_CERT=certs/localhost+2.pem

# Logging Configuration
# Log levels: silent, error, warn, info, debug, trace
# Default: error (production), debug (development)
# VITE_LOG_LEVEL=error

# For debugging production builds locally:
# VITE_LOG_LEVEL=debug

# For completely silent production:
# VITE_LOG_LEVEL=silent