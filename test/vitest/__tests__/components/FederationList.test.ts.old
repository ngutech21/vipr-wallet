import { describe, it, expect, vi } from 'vitest'
import { mount } from '@vue/test-utils'
import { Quasar } from 'quasar'
import FederationList from '@/components/FederationList.vue'

vi.mock('@/stores/federation', () => ({
  useFederationStore: () => ({
    federations: [{ inviteCode: '1', title: 'Test Federation', federationId: '12345' }],
    selectedFederation: null,
    selectFederation: vi.fn(),
    loadSelectedFederation: vi.fn(),
  }),
}))

describe('FederationList.vue', () => {
  it('renders federation name', () => {
    const wrapper = mount(FederationList, {
      global: {
        plugins: [Quasar],
      },
    })

    expect(wrapper.text()).toContain('Test Federation')
  })
})
